var somma = function(a, b) {
	return a+b;
}

var prodotto = function(a, b){
	return a*b;
}

var privata = function(a, b){
	return ('funzione privata');
}

exports.somma = somma; //rendiamo la funzione somma visibile e utilizzabile da chi importa questo modulo 
exports.prodotto = prodotto; //come sopra

//notiamo che la funzione "privata" non essendo esportata è di tipo private visibile solo all'interno di questo file